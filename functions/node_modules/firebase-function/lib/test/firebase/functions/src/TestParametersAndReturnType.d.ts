import { Flattable, ITypeBuilder } from "firebase-function";
export type TestParameters = {
    v1: string;
    v2: number[];
    v3: SubParameter;
};
export declare class SubParameter implements Flattable<boolean> {
    readonly v1: "a" | "b";
    constructor(v1: "a" | "b");
    static get typeBuilder(): ITypeBuilder<boolean, SubParameter>;
    get flatten(): boolean;
}
export declare class TestReturnType implements Flattable<{
    v1: string;
    v2: number;
}> {
    readonly v1_raw: string;
    readonly v2_raw: number;
    constructor(v1_raw: string, v2_raw: number);
    get flatten(): {
        v1: string;
        v2: number;
    };
}
