import { FirebaseRequest, ILogger, ObjectTypeBuilder } from "firebase-function";
import { TestParameters, TestReturnType } from "./TestParametersAndReturnType";
export declare class TestFirebaseRequest implements FirebaseRequest<TestParameters, TestReturnType> {
    parametersBuilder: ObjectTypeBuilder<{
        v1: string;
        v2: number[];
        v3: boolean;
    }, TestParameters>;
    constructor(logger: ILogger);
    execute(parameters: TestParameters): Promise<TestReturnType>;
}
