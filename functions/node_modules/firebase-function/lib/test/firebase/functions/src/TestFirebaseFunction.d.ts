import { FirebaseFunction, ILogger, ObjectTypeBuilder, AuthUser } from "firebase-function";
import { TestParameters, TestReturnType } from "./TestParametersAndReturnType";
export declare class TestFirebaseFunction implements FirebaseFunction<TestParameters, TestReturnType> {
    parametersBuilder: ObjectTypeBuilder<{
        v1: string;
        v2: number[];
        v3: boolean;
    }, TestParameters>;
    constructor(user: AuthUser | null, logger: ILogger);
    execute(parameters: TestParameters): Promise<TestReturnType>;
}
