import { type IHasher } from '../hasher';
import type { IMessageAuthenticator } from './IMessageAuthenticator';
export declare class HMAC implements IMessageAuthenticator {
    private readonly key;
    private readonly hasher;
    constructor(key: Uint8Array, hasher?: IHasher);
    sign(message: Uint8Array): Uint8Array;
    verify(message: Uint8Array, tag: Uint8Array): boolean;
}
