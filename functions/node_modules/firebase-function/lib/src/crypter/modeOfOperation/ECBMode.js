"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ECBMode = void 0;
class ECBMode {
    startEncryption() {
        return new Uint8Array();
    }
    combineEncryption(block, encrypt) {
        return encrypt(block);
    }
    finishEncryption() {
        return new Uint8Array();
    }
    startDecryption() {
        return new Uint8Array();
    }
    combineDecryption(block, decrypt) {
        return decrypt(block);
    }
    finishDecryption() {
        return new Uint8Array();
    }
}
exports.ECBMode = ECBMode;
