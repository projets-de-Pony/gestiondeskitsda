import { ICrypter } from './ICrypter';
import { type IModeOfOperation } from './modeOfOperation';
import { type IPadding } from './padding';
export declare class AES extends ICrypter {
    private readonly blockCrypter;
    private readonly modeOfOperation;
    protected readonly blockSize = 16;
    constructor(key: Uint8Array, modeOfOperation?: IModeOfOperation | null, padding?: IPadding);
    protected encryptBlocks(data: Uint8Array): Uint8Array;
    protected decryptBlocks(data: Uint8Array): Uint8Array;
}
