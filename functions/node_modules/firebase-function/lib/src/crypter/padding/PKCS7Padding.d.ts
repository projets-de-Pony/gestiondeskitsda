import type { IPadding } from './IPadding';
export declare class PKCS7Padding implements IPadding {
    addPadding(data: Uint8Array, blockSize: number): Uint8Array;
    removePadding(data: Uint8Array): Uint8Array;
}
