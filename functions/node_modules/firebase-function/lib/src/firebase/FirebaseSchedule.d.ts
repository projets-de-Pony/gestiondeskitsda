import * as functions from 'firebase-functions';
export interface FirebaseSchedule {
    execute(): Promise<void>;
}
export type FirebaseScheduleConstructor = new () => FirebaseSchedule;
export declare namespace FirebaseSchedule {
    function create(FirebaseSchedule: FirebaseScheduleConstructor, schedule: string, timezone: string, regions?: string[]): functions.CloudFunction<unknown>;
}
