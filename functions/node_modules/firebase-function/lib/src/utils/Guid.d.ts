import type { Flattable } from './Flattable';
export declare class Guid implements Flattable<string> {
    readonly guidString: string;
    constructor(guidString: string);
    static from(value: string): Guid;
    static generate(): Guid;
    get flatten(): string;
}
