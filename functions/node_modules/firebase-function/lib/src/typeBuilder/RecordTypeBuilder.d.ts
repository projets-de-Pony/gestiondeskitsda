import type { ILogger } from '../logger';
import type { ITypeBuilder } from './ITypeBuilder';
export declare class RecordTypeBuilder<V, T> implements ITypeBuilder<Record<string, V>, Record<string, T>> {
    private readonly builder;
    constructor(builder: ITypeBuilder<V, T>);
    build(value: Record<string, V>, logger: ILogger): Record<string, T>;
}
