import type { ITypeBuilder } from './ITypeBuilder';
export declare class TypeBuilder<V, T> implements ITypeBuilder<V, T> {
    readonly _build: (value: V) => T;
    constructor(_build: (value: V) => T);
    build(value: V): T;
}
