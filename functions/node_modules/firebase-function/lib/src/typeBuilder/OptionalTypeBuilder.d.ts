import type { ILogger } from '../logger';
import type { ITypeBuilder } from './ITypeBuilder';
export declare class OptionalTypeBuilder<V, T> implements ITypeBuilder<V | null, T | null> {
    private readonly builder;
    constructor(builder: ITypeBuilder<V, T>);
    build(value: V | null, logger: ILogger): T | null;
}
