import type { ILogger } from '../logger';
import type { ITypeBuilder } from './ITypeBuilder';
export declare class IntersectionTypeBuilder<V1, V2, T1, T2> implements ITypeBuilder<V1 & V2, T1 & T2> {
    private readonly builder1;
    private readonly builder2;
    constructor(builder1: ITypeBuilder<V1, T1>, builder2: ITypeBuilder<V2, T2>);
    build(value: V1 & V2, logger: ILogger): T1 & T2;
}
