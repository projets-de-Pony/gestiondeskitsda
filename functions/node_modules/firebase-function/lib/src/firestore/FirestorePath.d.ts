export declare class FirestorePath {
    private readonly components;
    constructor(...components: string[]);
    append(...components: string[]): void;
    appending(...components: string[]): FirestorePath;
    get isEmpty(): boolean;
    get parentPath(): FirestorePath;
    get lastComponent(): string;
    get fullPath(): string;
}
