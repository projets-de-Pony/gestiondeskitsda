import type { ILogger, LogLevel } from './ILogger';
export declare class Logger implements ILogger {
    verbose: boolean;
    private readonly properties;
    private readonly currentIndent;
    private constructor();
    get nextIndent(): Logger;
    get completeLog(): string;
    static start(functionName: string, details?: Record<string, unknown> | null, logLevel?: LogLevel, verbose?: boolean): Logger;
    log(functionName: string, details?: Record<string, unknown> | null, logLevel?: LogLevel): void;
}
