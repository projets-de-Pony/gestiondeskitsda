import type { LogLevel } from './ILogger';
export declare class LoggingProperty {
    private readonly functionName;
    private readonly logLevel;
    private readonly indent;
    private readonly details;
    constructor(functionName: string, logLevel: LogLevel, indent: number, details: Record<PropertyKey, unknown> | null);
    completeLog(verbose: boolean): string;
    private detailString;
    private prefix;
}
